services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: dwdb
      POSTGRES_PASSWORD: dwdb
      POSTGRES_DB: dwdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
